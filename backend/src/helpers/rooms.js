const optionsToRoomCreateQuery = ({
  id,
  attributes_id,
  description_id,
  elements_id,
  exits,
  items,
  short_description_id,
  long_description_id,
  name,
}) => {};

const optionsToRoomUpdateQuery = ({
  id,
  attributes_id,
  description_id,
  elements_id,
  exits,
  items,
  short_description_id,
  long_description_id,
  name,
}) => {};

module.exports = { optionsToRoomCreateQuery, optionsToRoomUpdateQuery };
